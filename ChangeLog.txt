done:
  - LDAP container
  - table with IP addresses ready
  - 'git clone' now works with git.zabbix.com
  - auto create 'zabbix' user in container if not exist
  - trunk backend tested with git commit

todo:
  - connect 7.0 GUI to open LDAP
  - connect to objects (ldap, proxy) using DNS. edit docker-compose

API todo:
  - change records per page per user
  - create global API token
  - install LDAP settings
  - disable unsupported items
  - create passive proxy
  - check and reimport outdated templates
